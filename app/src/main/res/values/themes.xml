<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Momentum" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/allerta</item>
        <!-- Other theme customizations here -->
    </style>

    <style name="customTheme" parent="Theme.Material3.DayNight">
        <!-- You might want to add the font here too if this theme is used -->
        <item name="android:fontFamily">@font/allerta</item>
    </style>

    <style name="Theme.Momentum" parent="Base.Theme.Momentum" />


</resources>
