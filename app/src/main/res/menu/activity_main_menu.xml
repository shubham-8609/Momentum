<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/add_todo_option"
        android:iconTint="@color/colorOnBackground"
        android:icon="@drawable/outline_add_24"
        android:title="New Todo"
        app:showAsAction="ifRoom"/>
        <item
            android:id="@+id/search_todo_option"
            android:icon="@drawable/outline_manage_search_24"
            android:title="Search Todo"
            android:iconTint="@color/colorOnBackground"
            app:showAsAction="ifRoom"/>
        <item
            android:id="@+id/settings_option"
            android:icon="@drawable/baseline_settings_24"
            android:iconTint="@color/colorOnBackground"
            android:title="Settings"/>
            <item
            android:id="@+id/exit_option"
                android:iconTint="@color/colorOnBackground"
            android:icon="@drawable/outline_exit_to_app_24"
            android:title="Exit"/>

</menu>